# CC Toast 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Claude Code 权限请求 / 回复完成时，在屏幕右下角弹出置顶且不抢焦点的 toast，点击跳回 CC 宿主窗口。

**Architecture:** CC hooks（Notification / Stop）触发一个即弃的 PowerShell 脚本：hook 入口脚本读 stdin JSON、回溯父进程链找宿主窗口 PID，然后 `Start-Process` 分离启动 WinForms 弹窗脚本。零常驻进程。

**Tech Stack:** Windows PowerShell 5.1 + .NET WinForms（系统自带，零安装）、Win32 API（Add-Type 内嵌 C#）、Claude Code hooks。

**Spec:** `docs/superpowers/specs/2026-07-20-cc-toast-design.md`

**注意事项（写给实现者）：**
- 本机是 Windows PowerShell 5.1：没有 `&&`/`||`、没有三元运算符。
- 弹窗参数中的 Message/Cwd 可能含空格、引号、中文 — 全部用 **base64** 传递，绕开引号地狱。
- toast 窗体必须同时具备：`ShowWithoutActivation` 重写 + `WS_EX_NOACTIVATE` 扩展样式（双保险不抢焦点）。
- `C:\Users\Administrator\.claude\settings.json` 里有用户的 env token 等敏感配置，**只允许新增 `hooks` 键，别的字段一个都不能动**，改前必须备份。
- GUI 无法自动断言，测试步骤以「精确命令 + 预期观察」形式给出，逐条人工核对。

---

## 文件结构

| 文件 | 职责 |
|---|---|
| `e:\claude code files\cc-toast\cc-toast.ps1` | 展示层：右下角 toast 窗口（创建：Task 1） |
| `e:\claude code files\cc-toast\cc-toast-hook.ps1` | 触发层：读 hook stdin、找宿主 PID、分离启动弹窗（创建：Task 2） |
| `C:\Users\Administrator\.claude\settings.json` | 新增 `hooks` 键（修改：Task 3） |

---

### Task 1: 展示层 cc-toast.ps1

**Files:**
- Create: `e:\claude code files\cc-toast\cc-toast.ps1`

- [ ] **Step 1: 写入 cc-toast.ps1 完整内容**

```powershell
# cc-toast.ps1 — Claude Code 右下角置顶提醒 toast
# 用法: powershell -STA -File cc-toast.ps1 -Type permission -MessageB64 <b64> -CwdB64 <b64> -HostPid <pid>
param(
    [ValidateSet('permission','idle')][string]$Type = 'permission',
    [string]$MessageB64 = '-',
    [string]$CwdB64 = '-',
    [int]$HostPid = 0
)

$ErrorActionPreference = 'Stop'
try {
    Add-Type -AssemblyName System.Windows.Forms
    Add-Type -AssemblyName System.Drawing

    Add-Type -ReferencedAssemblies System.Windows.Forms -TypeDefinition @'
using System;
using System.Runtime.InteropServices;
using System.Text;

public static class CCToastNative
{
    [DllImport("user32.dll")] public static extern bool SetForegroundWindow(IntPtr hWnd);
    [DllImport("user32.dll")] public static extern bool ShowWindow(IntPtr hWnd, int nCmdShow);
    [DllImport("user32.dll")] public static extern bool BringWindowToTop(IntPtr hWnd);
    [DllImport("user32.dll")] public static extern bool IsIconic(IntPtr hWnd);
    [DllImport("user32.dll")] public static extern void keybd_event(byte bVk, byte bScan, uint dwFlags, UIntPtr dwExtraInfo);
    [DllImport("user32.dll")] public static extern bool EnumWindows(EnumWindowsProc lpEnumFunc, IntPtr lParam);
    [DllImport("user32.dll")] public static extern int GetWindowText(IntPtr hWnd, StringBuilder lpString, int nMaxCount);
    [DllImport("user32.dll")] public static extern bool IsWindowVisible(IntPtr hWnd);
    public delegate bool EnumWindowsProc(IntPtr hWnd, IntPtr lParam);

    // 数当前已有几个 toast（按窗口标题精确匹配），用于向上堆叠
    public static int CountWindowsTitled(string title)
    {
        int count = 0;
        EnumWindows(delegate(IntPtr h, IntPtr l) {
            if (!IsWindowVisible(h)) return true;
            StringBuilder sb = new StringBuilder(256);
            GetWindowText(h, sb, 256);
            if (sb.ToString() == title) count++;
            return true;
        }, IntPtr.Zero);
        return count;
    }

    // 兜底：按标题片段找可见顶层窗口
    public static IntPtr FindVisibleWindowContaining(string fragment)
    {
        IntPtr found = IntPtr.Zero;
        EnumWindows(delegate(IntPtr h, IntPtr l) {
            if (!IsWindowVisible(h)) return true;
            StringBuilder sb = new StringBuilder(512);
            GetWindowText(h, sb, 512);
            if (sb.Length > 0 && sb.ToString().IndexOf(fragment, StringComparison.OrdinalIgnoreCase) >= 0)
            {
                found = h;
                return false;
            }
            return true;
        }, IntPtr.Zero);
        return found;
    }

    // 激活宿主窗口：空按键(Alt)解除前台锁定 -> 最小化则还原 -> 置前 -> 提升Z序
    public static void Activate(IntPtr hWnd)
    {
        keybd_event(0x12, 0, 0, UIntPtr.Zero); // Alt down
        keybd_event(0x12, 0, 2, UIntPtr.Zero); // Alt up (KEYEVENTF_KEYUP)
        if (IsIconic(hWnd)) ShowWindow(hWnd, 9); // SW_RESTORE
        SetForegroundWindow(hWnd);
        BringWindowToTop(hWnd);
    }
}

// 不抢焦点窗体：ShowWithoutActivation(Show时不激活) + WS_EX_NOACTIVATE(点击也不夺焦点)
public class NoActivateForm : System.Windows.Forms.Form
{
    protected override bool ShowWithoutActivation { get { return true; } }
    protected override System.Windows.Forms.CreateParams CreateParams
    {
        get
        {
            System.Windows.Forms.CreateParams cp = base.CreateParams;
            cp.ExStyle |= 0x08000000; // WS_EX_NOACTIVATE
            return cp;
        }
    }
}
'@

    function ConvertFrom-B64([string]$s) {
        if ([string]::IsNullOrEmpty($s) -or $s -eq '-') { return '' }
        try { return [Text.Encoding]::UTF8.GetString([Convert]::FromBase64String($s)) } catch { return '' }
    }
    $Message = ConvertFrom-B64 $MessageB64
    $Cwd     = ConvertFrom-B64 $CwdB64
    $proj = ''
    if ($Cwd) { $proj = Split-Path $Cwd -Leaf }

    if ($Type -eq 'permission') {
        $accent = [System.Drawing.Color]::FromArgb(230,126,34)   # 橙
        $titleText = 'Claude Code · 权限请求'
        if (-not $Message) { $Message = '有权限请求等待确认' }
        $lifeMs = 15000
    } else {
        $accent = [System.Drawing.Color]::FromArgb(46,204,113)   # 绿
        $titleText = 'Claude Code · 已完成'
        if (-not $Message) {
            if ($proj) { $Message = "$proj — 回复完成，等待输入" }
            else       { $Message = '回复完成，等待输入' }
        }
        $lifeMs = 6000
    }

    # 布局：主屏工作区右下角，已有 toast 时向上堆叠
    $w = 360; $h = 92; $margin = 12; $gap = 8
    $stackIndex = [CCToastNative]::CountWindowsTitled('CCToast')
    $wa = [System.Windows.Forms.Screen]::PrimaryScreen.WorkingArea
    $x = $wa.Right  - $w - $margin
    $y = $wa.Bottom - $h - $margin - ($stackIndex * ($h + $gap))

    $form = New-Object NoActivateForm
    $form.Text            = 'CCToast'
    $form.FormBorderStyle = 'None'
    $form.StartPosition   = 'Manual'
    $form.ShowInTaskbar   = $false
    $form.TopMost         = $true
    $form.Size            = New-Object System.Drawing.Size($w, $h)
    $form.Location        = New-Object System.Drawing.Point($x, $y)
    $form.BackColor       = [System.Drawing.Color]::FromArgb(30,30,30)

    $bar = New-Object System.Windows.Forms.Panel
    $bar.BackColor = $accent
    $bar.Size      = New-Object System.Drawing.Size(4, $h)
    $bar.Location  = New-Object System.Drawing.Point(0, 0)
    $form.Controls.Add($bar)

    $lblTitle = New-Object System.Windows.Forms.Label
    $lblTitle.Text      = $titleText
    $lblTitle.Font      = New-Object System.Drawing.Font('Microsoft YaHei UI', 10, [System.Drawing.FontStyle]::Bold)
    $lblTitle.ForeColor = [System.Drawing.Color]::White
    $lblTitle.Location  = New-Object System.Drawing.Point(16, 12)
    $lblTitle.Size      = New-Object System.Drawing.Size(($w - 28), 24)
    $form.Controls.Add($lblTitle)

    $lblMsg = New-Object System.Windows.Forms.Label
    $lblMsg.Text         = $Message
    $lblMsg.Font         = New-Object System.Drawing.Font('Microsoft YaHei UI', 9)
    $lblMsg.ForeColor    = [System.Drawing.Color]::FromArgb(190,190,190)
    $lblMsg.Location     = New-Object System.Drawing.Point(16, 40)
    $lblMsg.Size         = New-Object System.Drawing.Size(($w - 28), ($h - 52))
    $lblMsg.AutoEllipsis = $true
    $form.Controls.Add($lblMsg)

    # 点击：激活宿主窗口（PID -> 标题含项目名兜底 -> 放弃），然后关闭自己
    $script:form = $form
    $activate = {
        try {
            $hwnd = [IntPtr]::Zero
            if ($HostPid -gt 0) {
                $hp = Get-Process -Id $HostPid -ErrorAction SilentlyContinue
                if ($hp -and $hp.MainWindowHandle -ne [IntPtr]::Zero) { $hwnd = $hp.MainWindowHandle }
            }
            if ($hwnd -eq [IntPtr]::Zero -and $proj) {
                $hwnd = [CCToastNative]::FindVisibleWindowContaining($proj)
            }
            if ($hwnd -ne [IntPtr]::Zero) { [CCToastNative]::Activate($hwnd) }
        } catch {}
        $script:form.Close()
    }
    $form.Add_Click($activate)
    $lblTitle.Add_Click($activate)
    $lblMsg.Add_Click($activate)

    $timer = New-Object System.Windows.Forms.Timer
    $timer.Interval = $lifeMs
    $script:timer = $timer
    $timer.Add_Tick({ $script:timer.Stop(); $script:form.Close() })
    $timer.Start()

    [System.Windows.Forms.Application]::Run($form)
    exit 0
} catch {
    exit 0   # 任何异常静默退出，绝不反向干扰 CC
}
```

- [ ] **Step 2: 语法检查**

Run（PowerShell 工具）:
```powershell
$errs = $null; [System.Management.Automation.PSParser]::Tokenize((Get-Content -Raw "e:\claude code files\cc-toast\cc-toast.ps1"), [ref]$errs) | Out-Null; if ($errs.Count -eq 0) { "SYNTAX OK" } else { $errs }
```
Expected: `SYNTAX OK`

- [ ] **Step 3: 手动冒烟测试 — permission 样式**

Run:
```powershell
$b64 = [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes("Claude 请求执行 Bash 命令")); Start-Process powershell.exe -ArgumentList @('-NoProfile','-ExecutionPolicy','Bypass','-STA','-File','"e:\claude code files\cc-toast\cc-toast.ps1"','-Type','permission','-MessageB64',$b64)
```
Expected（人工观察）:
1. 右下角出现深色 toast，左侧橙色竖条，标题「Claude Code · 权限请求」，正文中文正常显示
2. 当前正在打字的窗口**焦点没有被抢走**
3. toast 压在全屏/置顶应用之上
4. 15 秒后自动消失

- [ ] **Step 4: 手动测试 — idle 样式 + 堆叠**

Run（连发 3 个）:
```powershell
1..3 | ForEach-Object { Start-Process powershell.exe -ArgumentList @('-NoProfile','-ExecutionPolicy','Bypass','-STA','-File','"e:\claude code files\cc-toast\cc-toast.ps1"','-Type','idle'); Start-Sleep -Milliseconds 800 }
```
Expected: 3 个绿色竖条 toast 从右下角**向上堆叠**、互不重叠，各自 6 秒后消失

- [ ] **Step 5: Commit**

```powershell
git add "cc-toast/cc-toast.ps1"; git commit -m "feat(cc-toast): 右下角置顶不抢焦点 toast 展示层"
```

---

### Task 2: 触发层 cc-toast-hook.ps1

**Files:**
- Create: `e:\claude code files\cc-toast\cc-toast-hook.ps1`

- [ ] **Step 1: 写入 cc-toast-hook.ps1 完整内容**

```powershell
# cc-toast-hook.ps1 — Claude Code hook 入口
# settings.json 中配置: powershell.exe ... -File cc-toast-hook.ps1 -Type permission|idle
# stdin 收到 CC 的 hook JSON（含 message/cwd），解析后分离启动 cc-toast.ps1，立即返回不阻塞 CC
param(
    [ValidateSet('permission','idle')][string]$Type = 'permission'
)

try {
    $raw = [Console]::In.ReadToEnd()
    $msg = ''; $cwd = ''
    if ($raw) {
        try {
            $data = $raw | ConvertFrom-Json
            if ($data.message) { $msg = [string]$data.message }
            if ($data.cwd)     { $cwd = [string]$data.cwd }
        } catch {}
    }

    # 顺父进程链回溯，找第一个有主窗口句柄的祖先作为宿主（不写死应用名，
    # 兼容 VS Code / Cursor / Windsurf / Trae / Windows Terminal 等）
    $hostProcId = 0
    try {
        $current = $PID
        for ($i = 0; $i -lt 15; $i++) {
            $wp = Get-CimInstance Win32_Process -Filter "ProcessId = $current" -ErrorAction SilentlyContinue
            if (-not $wp -or -not $wp.ParentProcessId) { break }
            $parentId = [int]$wp.ParentProcessId
            $pp = Get-Process -Id $parentId -ErrorAction SilentlyContinue
            if ($pp -and $pp.MainWindowHandle -ne [IntPtr]::Zero) { $hostProcId = $parentId; break }
            $current = $parentId
        }
    } catch {}

    function To-B64([string]$s) {
        if ([string]::IsNullOrEmpty($s)) { return '-' }
        return [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes($s))
    }

    $toast = Join-Path $PSScriptRoot 'cc-toast.ps1'
    $argList = @(
        '-NoProfile','-ExecutionPolicy','Bypass','-STA','-WindowStyle','Hidden',
        '-File', "`"$toast`"",
        '-Type', $Type,
        '-MessageB64', (To-B64 $msg),
        '-CwdB64', (To-B64 $cwd),
        '-HostPid', $hostProcId
    )
    Start-Process -FilePath 'powershell.exe' -ArgumentList $argList -WindowStyle Hidden
} catch {}
exit 0
```

- [ ] **Step 2: 语法检查**

Run:
```powershell
$errs = $null; [System.Management.Automation.PSParser]::Tokenize((Get-Content -Raw "e:\claude code files\cc-toast\cc-toast-hook.ps1"), [ref]$errs) | Out-Null; if ($errs.Count -eq 0) { "SYNTAX OK" } else { $errs }
```
Expected: `SYNTAX OK`

- [ ] **Step 3: 管道模拟 hook 输入测试**

Run:
```powershell
'{"session_id":"test","cwd":"e:\\claude code files\\cc-toast","message":"Claude needs your permission to use Bash"}' | powershell.exe -NoProfile -ExecutionPolicy Bypass -File "e:\claude code files\cc-toast\cc-toast-hook.ps1" -Type permission
```
Expected:
1. 命令**立即返回**（不等 toast 消失），exit code 0
2. 右下角出现橙色 toast，正文为 "Claude needs your permission to use Bash"

- [ ] **Step 4: 点击跳转测试**

在 VS Code 集成终端里运行 Step 3 的命令，然后切到别的应用（比如浏览器）把 VS Code 挡住，点击 toast。
Expected: VS Code 窗口被置前并获得焦点，toast 消失。（父链回溯在 VS Code 终端里应命中 Code.exe；如果最小化 VS Code 再点，也要能还原置前）

- [ ] **Step 5: Commit**

```powershell
git add "cc-toast/cc-toast-hook.ps1"; git commit -m "feat(cc-toast): hook 入口 — stdin 解析/父链回溯/分离启动"
```

---

### Task 3: hooks 配置接入 settings.json

**Files:**
- Modify: `C:\Users\Administrator\.claude\settings.json`（只新增顶层 `hooks` 键）

- [ ] **Step 1: 备份**

Run:
```powershell
Copy-Item "C:\Users\Administrator\.claude\settings.json" "C:\Users\Administrator\.claude\settings.json.bak-cctoast"; "BACKUP OK"
```
Expected: `BACKUP OK`

- [ ] **Step 2: 新增 hooks 键**

用 Edit 工具在 `settings.json` 顶层 JSON 中新增以下键（与 `"env"`、`"permissions"` 平级；**不得改动任何现有字段**）：

```json
"hooks": {
  "Notification": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "powershell.exe -NoProfile -ExecutionPolicy Bypass -File \"e:\\claude code files\\cc-toast\\cc-toast-hook.ps1\" -Type permission",
          "timeout": 10
        }
      ]
    }
  ],
  "Stop": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "powershell.exe -NoProfile -ExecutionPolicy Bypass -File \"e:\\claude code files\\cc-toast\\cc-toast-hook.ps1\" -Type idle",
          "timeout": 10
        }
      ]
    }
  ]
}
```

说明：Notification 事件除权限请求外，CC 空闲 60s+ 的「等待输入」提醒也会走此事件，同样按 permission 样式显示，属于可接受行为（都是「需要你回终端看一眼」）。

- [ ] **Step 3: 验证 JSON 合法且旧字段完好**

Run:
```powershell
$j = Get-Content -Raw "C:\Users\Administrator\.claude\settings.json" | ConvertFrom-Json; $ok = ($null -ne $j.env.ANTHROPIC_BASE_URL) -and ($null -ne $j.permissions) -and ($null -ne $j.hooks.Notification) -and ($null -ne $j.hooks.Stop); if ($ok) { "SETTINGS OK" } else { "SETTINGS BROKEN - restore from .bak-cctoast" }
```
Expected: `SETTINGS OK`（若输出 BROKEN，立即用备份还原再重做 Step 2）

- [ ] **Step 4: Commit（脚本仓库侧记录配置片段）**

hooks 配置在 C 盘用户目录、不在 git 仓库内。在 `cc-toast\README.md` 写入安装说明（含 Step 2 的完整 JSON 片段和手动测试命令），提交：

```powershell
git add "cc-toast/README.md"; git commit -m "docs(cc-toast): 安装说明与 hooks 配置片段"
```

---

### Task 4: 端到端验收

- [ ] **Step 1: 重启 CC 会话使 hooks 生效**（hooks 在会话启动时快照，改完必须开新会话）

- [ ] **Step 2: 权限请求场景**

在新 CC 会话里让 Claude 执行一条不在 allow 列表的命令（例如 `format-test-dummy --help` 这类必然触发确认的），CC 弹权限确认时观察。
Expected: 右下角橙色 toast，内容为权限请求描述；点击 toast 回到 CC 宿主窗口。

- [ ] **Step 3: 空闲/完成场景**

随便问 CC 一个问题，等它回复完成。
Expected: 回复结束时右下角绿色 toast（6 秒自动消失）。

- [ ] **Step 4: 干扰验证**

触发 toast 时正在别的应用里打字。
Expected: 输入焦点不被抢走；toast 显示在最上层。

- [ ] **Step 5: 收尾 Commit**

若验收中有微调（坐标、时长、文案），一并提交：
```powershell
git add "cc-toast"; git commit -m "fix(cc-toast): 端到端验收微调"
```

---

## 自审记录

- **Spec 覆盖**：右下角置顶 ✓（Task 1）、不抢焦点双保险 ✓（Task 1 NoActivateForm）、堆叠 ✓、两类事件与配色/时长 ✓、父链回溯不写死应用 ✓（Task 2）、激活序列含前台锁定绕过 ✓（Task 1 Activate）、标题匹配兜底 ✓、hooks 合并且不动敏感字段 ✓（Task 3 备份+校验）、异常静默 ✓、E2E ✓（Task 4）
- **占位符**：无 TBD/TODO，所有代码步骤含完整代码
- **一致性**：`-Type/-MessageB64/-CwdB64/-HostPid` 参数名在 Task 1 定义与 Task 2 调用一致；窗口标题 `CCToast` 与堆叠计数一致
